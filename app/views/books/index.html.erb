<div>
 <%=link_to "create new book", new_book_path%>
 <% @books.each do |u| %>
  <div class="row ">
  	<div class="container">
    <div class="col-xs-8 col-xs-offset-2">
      <div class="well well-lg">
        <div class="article-body">
         <%= link_to u.title  %>
          <div class="article-meta-details">
            <small>last updated: <%= time_ago_in_words(u.updated_at) %> ago</small>  
          </div>
        </div>
       
          <div class="article-actions">
            <%= link_to "Edit book", edit_book_path(u), class: "btn btn-xs btn-primary" %> 
            <%= link_to "Delete book", book_path(u), method: :delete, 
                                                data: { confirm: "Are you sure you want to delete this profile?" }, 
                                                class: "btn btn-xs btn-danger" %>
          </div>
      </div>
    </div>
   </div>
  </div>
  </div>  
<% end %>